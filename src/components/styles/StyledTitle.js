import styled from "styled-components";

export const StyledTitle = styled.h2`
    color: ${(props) => props.color};
    text-align: center;
    margin: .7m;
    padding-bottom: 2rem;
    font-weight: 600;
`