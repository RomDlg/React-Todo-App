import styled from "styled-components";

export const StyledTitle = styled.h2`
    color: ${(props) => props.color};
    text-align: center;
    margin: .5rem;
    padding-bottom: 2rem;
    font-weight: 600;
`